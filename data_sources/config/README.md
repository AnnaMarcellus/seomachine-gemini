# Configuration Directory

Place your `.env` file here with your API credentials.

```bash
cp ../../.env.example ./.env
# Edit .env with your actual credentials
```

**Never commit this file to version control!**
